import 'package:flutter/material.dart';

class Vaporwave extends StatelessWidget {
  const Vaporwave({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
